{{-- <div class="sort-items">
    <form class="form-sort-items" action="{{ route($parent_link) }}" method="GET" id="sort-items">
        <p class="fort-sort-title">Сортировка</p>
        <select name="sort" class="sort-items-form"
                onchange="document.getElementById('sort-items').submit()"
        >
            <option value="new-items">спочатку нові</option>
            <option value="old-items">спочатку старі</option>
            <option value="alphabet-start">за алфавітом а-я</option>
            <option value="alphabet-end">за алфавітом я-а</option>
        </select>
    </form>
</div> --}}
